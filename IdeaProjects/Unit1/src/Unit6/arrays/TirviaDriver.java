package Unit6.arrays;

public class TirviaDriver {
    public static void main(String[] args) {

        //call method to get info from file
        //ask for name(scanner)
        //"welcome thing + name"
        //ask if they want to see instructions(0 for no, 1 for yes)(scanner)
            //if yes then show instructions
            //else call tostring
                //boolean
                //while boolean = false
                    //show question
                    //ask for answer choice(scanner)
                    //call checkanswer from tirviagame{souts the answer part}
                    //sout you have + points + answer streak

                    //"continue? ask yes or no
                    //if yes then boolean = true


    }
}
